import React, { Component } from 'react'
import MaterialIcon from 'material-icons-react'
import Button from 'react-bootstrap/Button'

// import {  } from './editar-arquitetura.styled'

const initialState = {
  myProdutosService: Object,
}

export default class EditarArquitetura extends Component {
  state = { ...initialState }

  componentDidMount() {  }

  constructor() {
    super()
  }

  render() {
    const {  } = this.state;

    return (
      <div>editar</div>
    )
  }
}